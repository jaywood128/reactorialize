<%= form_with(model: tutorial_module, local: true) do |form| %>
  <% if tutorial_module.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tutorial_module.errors.count, "error") %> prohibited this tutorial_module from being saved:</h2>

      <ul>
      <% tutorial_module.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
